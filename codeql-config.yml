name: CodeQL Configuration for Python
paths:
  - 'src/*.py'  # Scan Python files in src/
  - '*.py'      # Catch any Python files
paths-ignore:
  - 'tests/**'   # Skip test files
  - 'docs/**'    # Skip documentation
queries:
  - uses: security-and-quality
  - uses: ./queries/python-security.ql